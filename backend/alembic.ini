[alembic]
script_location = alembic
prepend_sys_path = .
path_separator = os

# Revisions are numbered, not hash-named: 0001_enable_extensions.py sorts and
# reads better than a random hex slug when there are eventually eighty of them.
file_template = %%(rev)s_%%(slug)s

# The URL is intentionally absent. alembic/env.py reads it from app.config so
# there is exactly one place where the DSN is defined (§16.3).

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARNING
handlers = console
qualname =

[logger_sqlalchemy]
level = WARNING
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
